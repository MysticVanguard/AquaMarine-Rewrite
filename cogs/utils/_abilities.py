class _Ability:

    def __init__(ability_description, ):
            
        # Types
        """
        give_shield(amount, *target) -> temporary bonus hp
        gain_stat(stat, amount, num_turns, *target) -> can increase max hp
        stun(num_turns, *target)

        heal(amount, num_turns, *target) -> can only go up to max hp
        deal(damage_type, amount, num_turns, *target) -> can go to 0

        debuff_stat(stat, amount, num_turns, *target)


        """